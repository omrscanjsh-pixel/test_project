# Atlas Connection String
# 비밀번호에 # @ : / ? 등 특수문자가 있으면 URL 인코딩 필요 (예: # → %23, @ → %40)
MONGODB_URI=mongodb+srv://omrscanjsh_db_user:YOUR_PASSWORD@cluster0.clnqy7d.mongodb.net/feed?retryWrites=true&w=majority&appName=Cluster0

# 데이터베이스 이름
DB_NAME=feed

# 서버 포트
PORT=8080
